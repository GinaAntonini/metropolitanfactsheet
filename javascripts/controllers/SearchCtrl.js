"use strict";

app.controller("SearchCtrl", function($location, $rootScope, $scope){
    // $scope.enterPush = (e) => {
    //     if(event.keyCode === 13){
    //         PropertyInfoService.searchProperties(e.target.value).then((results) => {
    //             $scope.properties = results.data.results;
    //         }).catch((err) => {
    //             console.log("error in searchProperties", err);
    //         });
    //     }
    // };

});